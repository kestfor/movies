// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: titles.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getTitleIDByTMDB = `-- name: GetTitleIDByTMDB :one
SELECT id
FROM titles
WHERE tmdb_id = $1 AND media_type = $2
`

type GetTitleIDByTMDBParams struct {
	TmdbID    int64
	MediaType MediaType
}

func (q *Queries) GetTitleIDByTMDB(ctx context.Context, arg GetTitleIDByTMDBParams) (int64, error) {
	row := q.db.QueryRow(ctx, getTitleIDByTMDB, arg.TmdbID, arg.MediaType)
	var id int64
	err := row.Scan(&id)
	return id, err
}

const upsertTitleSnapshot = `-- name: UpsertTitleSnapshot :one
INSERT INTO titles (
    tmdb_id,
    media_type,
    title,
    original_title,
    poster_path,
    release_year,
    genres,
    overview,
    cached_at
)
VALUES ($1, $2, $3, $4, $5, $6, $7::jsonb, $8, now())
ON CONFLICT (tmdb_id, media_type) DO UPDATE
SET title = EXCLUDED.title,
    original_title = EXCLUDED.original_title,
    poster_path = EXCLUDED.poster_path,
    release_year = EXCLUDED.release_year,
    genres = EXCLUDED.genres,
    overview = EXCLUDED.overview,
    cached_at = now()
RETURNING id
`

type UpsertTitleSnapshotParams struct {
	TmdbID        int64
	MediaType     MediaType
	Title         string
	OriginalTitle pgtype.Text
	PosterPath    pgtype.Text
	ReleaseYear   pgtype.Int4
	Column7       []byte
	Overview      pgtype.Text
}

func (q *Queries) UpsertTitleSnapshot(ctx context.Context, arg UpsertTitleSnapshotParams) (int64, error) {
	row := q.db.QueryRow(ctx, upsertTitleSnapshot,
		arg.TmdbID,
		arg.MediaType,
		arg.Title,
		arg.OriginalTitle,
		arg.PosterPath,
		arg.ReleaseYear,
		arg.Column7,
		arg.Overview,
	)
	var id int64
	err := row.Scan(&id)
	return id, err
}
