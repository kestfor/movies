// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: feed.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listFeedRatings = `-- name: ListFeedRatings :many
WITH visible_ratings AS (
    SELECT r.id
    FROM ratings r
    JOIN friendships f ON f.status = 'accepted'
        AND (
            (f.requester_id = $1 AND f.addressee_id = r.user_id)
            OR (f.addressee_id = $1 AND f.requester_id = r.user_id)
        )
    WHERE ($2::timestamptz IS NULL OR (r.created_at, r.id) < ($2::timestamptz, $3::bigint))
    ORDER BY r.created_at DESC, r.id DESC
    LIMIT $4
)
SELECT
    r.id,
    r.user_id,
    r.title_id,
    r.avg_score,
    r.created_at,
    r.updated_at,
    u.id AS author_id,
    u.tg_id AS author_tg_id,
    u.username AS author_username,
    u.first_name AS author_first_name,
    u.photo_url AS author_photo_url,
    u.created_at AS author_created_at,
    t.id AS title_id,
    t.tmdb_id,
    t.media_type,
    t.title,
    t.original_title,
    t.poster_path,
    t.release_year,
    t.genres,
    t.overview,
    c.code AS criterion_code,
    rs.score AS criterion_score
FROM visible_ratings vr
JOIN ratings r ON r.id = vr.id
JOIN users u ON u.id = r.user_id
JOIN titles t ON t.id = r.title_id
JOIN rating_scores rs ON rs.rating_id = r.id
JOIN criteria c ON c.id = rs.criterion_id
ORDER BY r.created_at DESC, r.id DESC, c.sort_order, c.id
`

type ListFeedRatingsParams struct {
	RequesterID int64
	Column2     pgtype.Timestamptz
	Column3     int64
	Limit       int32
}

type ListFeedRatingsRow struct {
	ID              int64
	UserID          int64
	TitleID         int64
	AvgScore        pgtype.Numeric
	CreatedAt       pgtype.Timestamptz
	UpdatedAt       pgtype.Timestamptz
	AuthorID        int64
	AuthorTgID      int64
	AuthorUsername  pgtype.Text
	AuthorFirstName string
	AuthorPhotoUrl  pgtype.Text
	AuthorCreatedAt pgtype.Timestamptz
	TitleID_2       int64
	TmdbID          int64
	MediaType       MediaType
	Title           string
	OriginalTitle   pgtype.Text
	PosterPath      pgtype.Text
	ReleaseYear     pgtype.Int4
	Genres          []byte
	Overview        pgtype.Text
	CriterionCode   string
	CriterionScore  int16
}

func (q *Queries) ListFeedRatings(ctx context.Context, arg ListFeedRatingsParams) ([]ListFeedRatingsRow, error) {
	rows, err := q.db.Query(ctx, listFeedRatings,
		arg.RequesterID,
		arg.Column2,
		arg.Column3,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListFeedRatingsRow
	for rows.Next() {
		var i ListFeedRatingsRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.TitleID,
			&i.AvgScore,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.AuthorID,
			&i.AuthorTgID,
			&i.AuthorUsername,
			&i.AuthorFirstName,
			&i.AuthorPhotoUrl,
			&i.AuthorCreatedAt,
			&i.TitleID_2,
			&i.TmdbID,
			&i.MediaType,
			&i.Title,
			&i.OriginalTitle,
			&i.PosterPath,
			&i.ReleaseYear,
			&i.Genres,
			&i.Overview,
			&i.CriterionCode,
			&i.CriterionScore,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
